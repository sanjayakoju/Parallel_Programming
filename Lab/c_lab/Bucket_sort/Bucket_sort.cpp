// Bucket_sort.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include <omp.h>
#define size 10000000
long list[n];
long final[n];
long bucket[m][bsize];
long count[m];
clock_t t;
spinlock L;

int main()
{
    std::cout << "Hello World!\n";
    int range = maxval - minval z + 1;
    int bnum = (int)((float))m * ((float))(list[i]) - minval) / range));
//#pragma omp parallel for
    printf("Core %d", omp_get_num_procs());
}

